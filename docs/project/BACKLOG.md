# Backlog

> 这里的事项不是当前任务，不得顺手实现。优先级需由用户重新确认。

## 分支与发布治理

- 设计 `wip/m10-project-context-audit` 与 `fix/existing-draft-baseline` 的安全集成方案；两者当前互不包含。
- 审核 `6c93e77` 中约 47 个文件的 M10 WIP，决定拆分、保留或废弃；不得直接整体合并。
- 处理陈旧 `.codex-dev.pid` 的生命周期与服务启动脚本，但不得杀死其他 Node 进程。
- 在用户验收和授权后再评估 Release Candidate/M11；当前不得部署。

## 用户体验与真实性

- 普通用户 UI 清理 M2/M3/M5/M8/M10 等内部里程碑标签，改成可使用/部分可用/需配置/尚未测试等状态。
- 全站移除真实项目中的案例数据、旧 Mock 成功提示和跨项目 fallback。
- 完成创建卡片、材料选择、对话区域响应式与可访问性回归；仅在当前唯一任务完成后排期。

## 项目与协作

- Workspace、Membership、Reviewer/审核员分配及权限矩阵的 M10 审计与实现。
- 项目上下文、成员权限与 owner 隔离的全链路验收。
- 项目删除的人工确认、依赖对象处置与恢复策略验收。

## 科研输出

- 六个产品 Skill 逐个完成真实 Adapter、材料授权、Action Proposal、AITask、候选版本闭环。
- 图件工作台的真实 runner、完整图型目录（含小提琴图等）、版本、导出和项目级验收。
- PPT 真实渲染、打开验证、版本与导出闭环。
- DOCX 导出的前置/正文/后置顺序、引用、图片和下载验收。
- 审稿返修、文献证据和诊断四种入口的去 Mock 与端到端验收。

## 数据与模型

- 在明确批准后对齐两个 worktree 的 migration 0019—0022；任何新增 migration 必须先停下确认。
- 清理 Provider/Model 的 legacy `mock` ID，必须以官方目录或真实列表迁移，不静默替换模型。
- DeepSeek 凭据配置、单模型单角色单次调用和 Secret 泄漏检查；只有用户允许后执行。
