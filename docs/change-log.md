# 变更日志

## 2026-07-29 · M5—M7 连续实现与审核收尾

- M5 完成 TXT/CSV/BibTeX/RIS/DOCX/文本 PDF/XLSX/图片解析、项目知识检索、AES-GCM 用户凭据、OpenAI/DeepSeek Adapter、六 Skill 共用请求、有界 Runner、候选版本和部分失败保留。
- 修复 I-017 预算暂停自动恢复和 I-018 Provider 安全错误字段；没有真实测试凭据时不发起或伪报供应商调用。
- M6 新增 Claim/Evidence 到最新成功 MaterialChunk 的严格核验、冲突和高风险导出阻断、高级审阅、Data Availability、投稿准备和真实 DOCX。
- DOCX 经 OOXML 解包测试和本机 Microsoft Word 不可见只读打开验证，标题、章节、段落和参考文献可读取。
- M7 新增决定信拆分、Reviewer/Comment、Revision Task、同意/部分同意/不同意策略、未完成实验警告、Response Draft 追加版本、用户确认、修改核对和 Response Letter DOCX。
- 当前停在 M7 审核门；未进入 M8 科研图件、M9 PPTX、M10 发布候选或 M11 部署。

## 2026-07-28 · M5-B2D 会话压缩、恢复与权限回归

- 会话消息读取改为有界分页，默认 50 条、最多 100 条，并提供向前分页状态，避免长期会话一次加载全部消息。
- 未摘要消息超过 24 条时生成追加式压缩计划：单批最多 16 条连续来源，始终保留最近 8 条；摘要不删除、覆盖或伪装原消息。
- 摘要来源必须属于同一会话并构成连续区间，防止跳过中间消息后错误推进压缩覆盖位置。
- 从持久化提案派生 `WAIT_FOR_USER / READY_TO_QUEUE / TERMINAL` 恢复快照；失败重试必须复用幂等键，恢复结果不包含模型执行动作。
- B2 专项测试 14/14 通过；无新 Schema 或 migration，I-017 预算暂停问题仍按计划留到真实 Runner 批次。

## 2026-07-28 · M5-B2C 操作提案确认门持久化

- 新增 ToolIntent、Action Proposal 和一次性用户决定三张表及 0008 增量迁移；迁移只生成、未执行。
- 提案建立前核验当前用户、项目、会话和材料授权范围；软删除或跨项目材料不能进入提案。
- 未确认提案保持 `WAITING_FOR_USER`；确认仅转为 `READY_TO_QUEUE`，拒绝转为 `TERMINAL`，均不会创建或执行 AI Task。
- 用户决定不可覆盖并支持幂等重放；0000→0008 全新内存迁移链为 66 张表，专项行为测试 3/3 通过。

## 2026-07-28 · M5-B2B 长期会话持久化

- 新增会话、消息与派生摘要三张增量表及 0007 迁移文件；迁移只生成、未在当前数据库执行。
- 新增所有者与项目双重隔离的 Repository/API，覆盖创建、继续会话、幂等消息、来源绑定摘要和归档。
- 摘要固定标记为 `DERIVED_NOT_USER_CONFIRMED`，不得冒充用户确认事实；归档会话拒绝后续写入。
- 0000→0007 全新内存迁移链为 63 张表，专项行为测试 3/3 通过；未调用真实模型、真实 Key 或执行 Skill。

## 2026-07-28 · M5-B2A 审核反馈修正

- 修复 demo D1 返回空提纲时覆盖中栏演示章节的问题；空提纲仅在 demo 项目回退到既有章节，不写回数据库。
- Conversation Agent 移除重复的六 Skill Prompt 选择条，只保留纯对话与操作提案。
- 六个 Skill 卡片成为 Prompt 入口：点击可用 Skill 后自动切换到“对话 Agent”，并把对应默认 Prompt 放入输入框供用户继续修改。
- 未改变 Skill 启用条件、任务执行、确认门、数据库 Schema、Provider 或部署。

## 2026-07-28 · M5-B2A 对话优先 AI 工作台与 Conversation Agent 基础

- 按用户确认的新路线将已完成的 Storage Adapter 与上传安全基础重分类为 M5-B3A，代码和原提交不重做。
- AI 工作台新增“对话 Agent / Skill 任务”顶层双页签；原六个产品 Skill、材料授权、证据、复核和任务记录完整保留。
- 新增六 Skill 默认 Prompt、ToolIntent、有限会话摘要、Action Proposal 和显式用户确认状态；确认只记录决定，不执行真实任务。
- 当前未新增 Schema/migration、会话持久化、真实 Provider/Key、文件解析、真实模型调用、DOCX 或部署。

## 2026-07-28 · M5-B2A Storage Adapter 与上传安全基础

- 新增与业务解耦的 `StorageAdapter`、内存实现和本地 Miniflare R2 实现；本地对象存储只在显式开发开关下启用，不创建远程 Bucket，也不暴露 `.wrangler` 路径。
- 新增 25 MB 硬限制、文件名/扩展名/MIME/Magic Bytes 联合探测，支持 PDF、DOCX、TXT、CSV、XLSX、JPG/JPEG、PNG、BibTeX、RIS，拒绝旧 DOC/XLS、路径穿越和可执行文件。
- 新增 0006 增量迁移，记录不可覆盖的 `material_objects` 与 `material_storage_events`；上传以 `PENDING_UPLOAD → STORED` 和材料 `AWAITING_PARSE` 收尾。
- 上传 API 使用真实 Session 和 owner-scoped 项目校验；对象键仅由服务端 ID 组成，跨用户返回 404，幂等请求不重复创建成功对象，最终数据库失败会删除刚写入对象或保留补偿记录。
- 现有上传创建页接入真实上传、取消、失败和等待解析状态；不展示解析进度，也不声称进入知识库。
- 登记 M5-FU-01 与 M5-FU-02；本批未实现 Parser、真实模型、真实 Key、自动脱敏、PPTX 或部署。

## 2026-07-28 · M5-B1 真实执行共享契约

- 用户批准进入 M5；建立 `m5/real-services-foundation` 分支并冻结 B1—B7 小批次、依赖和停止条件。
- 固化六个产品 Skill 的统一上下文与结构化结果，保留已确认诊断卡、用户授权材料、版本和证据边界。
- 增加与供应商无关的 Provider Adapter、凭据解析、连接测试/执行结果、超时和注册表接口；本批没有实现或伪装真实供应商调用。
- 固化标准/严格/自定义 2/3/4 模型和 2/4/5 次调用上限，以及任务恢复、等待用户和终态分类。
- 本批没有 Schema/migration、真实 Key、外部模型调用、真实文件解析、DOCX/PPTX 或部署。

## 2026-07-28 · M4-H2 完成 CORE-01 真实认证

- 增加真实邮箱/手机号注册、输入标准化与唯一性检查；密码使用 Web Crypto PBKDF2-SHA256、独立随机盐和版本化参数保存，数据库与 API 均不保存或返回明文密码。
- 增加注册、登录、退出和 Session API；Session 使用高熵随机 Token，D1 只保存 SHA-256 哈希，Cookie 为 HttpOnly、SameSite=Lax、Path=/，生产环境启用 Secure，并支持到期与撤销。
- 增加 0005 增量迁移，为 users 补充 `password_hash` 和 `last_login_at`；当前本地 D1 按既有迁移链增量应用，未重建或清空数据库。
- 项目页面与 M3/M4 API 统一从服务器 Session 获取当前用户；匿名页面重定向登录、匿名 API 返回 401、跨用户资源返回 404，前端 `user_id` 和旧测试 Header 均不能决定所有者。
- CORE-01 更新为通过，M4 恢复 `M4_PASS` 并继续停在审核门；未进入 M5、调用真实模型、接收真实 Key、执行真实脱敏、生成 PPTX 或部署。

## 2026-07-28 · M4-H1 解决 I-015 本地 D1 基线

- 用户批准 `M4_PASS_WITH_BLOCKER` 和方案 A；旧 42 表/0 台账的 Miniflare D1 状态完整归档到 `E:\论文系统\local-d1-archives\20260728-102348-I-015-9391D72F`，归档库 SHA-256 保持 `9391D72F0987F2B3080152582D41DA898214FCEF35250F547A4F4282CEC15CDD`。
- 使用当前 `DB` binding、`site-creator-d1` 和 `wrangler.m3-local.jsonc` 创建新的持续存在本地 D1；标准 Wrangler 迁移按 0000→0004 完成，台账 5 条、业务表 58 张，重复执行报告无待执行迁移。
- 新增持久化 D1 启动、只读检查和 API 验收脚本；唯一标记 `I015_PERSISTENCE_20260728_1052` 覆盖诊断、父子任务、审阅、六种隐私模式、13 种 PPT 场景和模型配置，并在应用重启后保留。
- 运行时复测确认匿名请求 401、跨用户资源请求 404；两次准确记录的 Vinext/workerd 进程均已停止，3000 端口已释放，临时日志未进入仓库。
- I-015 与 R-023 已关闭，M4 结论更新为 `M4_PASS`；仍停在 M4 审核门，没有进入 M5、调用真实模型、接收真实 Key、执行真实脱敏、生成 PPTX 或部署。

## 2026-07-28 · M4 V0.5.1 持久化与安全契约完成

- 以七个独立提交完成 M4-B1—B7，没有推倒 M2/M3 前端。
- 增加五种项目起点与三个最低问题、材料元数据、渐进式诊断、任务级就绪、AI Task/审阅/采用版本的持久化契约。
- 增加七类材料画像、六种隐私模式、处理副本、伪匿名引用、外传计划和分析保真阻断。
- 增加 13 种 PPT 场景及来源快照绑定，以及 Provider、Model、ExecutionProfile、平台/用户凭据元数据；M4 API 明确拒绝明文 Key。
- 0000→0004 在全新内存 SQLite 顺序通过并产生 58 张业务表；旧本地 D1 只读预检仍显示 42 张业务表、0 条迁移台账，未盲目修改。
- 请求级集成测试验证匿名 401、跨用户隔离和 M4 各域写入；M4 30 项、既有回归 25 项、API 集成 2 项、全仓 ESLint、类型检查和构建通过。
- 当前停在 M4 审核门；没有真实模型调用、真实 API Key、真实自动脱敏、真实 PPTX、M5 或部署。

## 2026-07-28 · V0.5.1 M4 需求基线与批次冻结

- 将 `docs/requirements/论文AI科研全流程平台需求说明_V0.5.1_M4修订版.txt` 标记为当前有效需求基线，旧需求只读保留。
- 确认 M0—M3 已完成，当前进入 M4；M2/M3 前端和视觉体系继续作为不可推倒的增量基线。
- 将剩余 M4 拆为 B1—B7：共享契约、所有权、诊断、AI 任务/审阅、隐私、PPT/BYOK、权限与审核收尾。
- 识别本地 D1 已包含 42 张业务表但 `d1_migrations` 台账无记录的漂移，禁止在未完成预检前盲目重跑 migration。
- M4 仍禁止真实模型、真实 API Key、真实自动脱敏、真实 PPTX、生产部署和 M5 能力。

## 2026-07-27 · M3 渐进式诊断与多模型编排增量 Mock

- 将诊断卡改为“快速开始、AI 引导梳理、材料提取、完整填写”四种渐进入口；创建项目最低只问三个基础问题。
- 增加单问题引导、标准/深度模式、不知道/跳过/不适用/AI 推测、字段状态与来源、停止条件和任务级就绪 Mock。
- 增加五种模型角色、标准/严格/自定义编排上限，以及生成/审阅/验证模型的独立记录。
- 增加平台凭据与用户凭据契约、模型与 API 设置页、掩码 Key 演示、执行前披露、冲突意见和八种失败状态。
- M3 不接收真实 Key；输入框保持禁用，不连接供应商，不写浏览器存储或日志。
- 新增能力均有功能开关；没有修改 Schema、执行迁移、接入真实 AI/Skill 或部署。

## 2026-07-27 · V0.4.3 双模型生成与独立审阅 Mock

- 在 M2 AI 工作台中增量增加可关闭的“AI 复核”标签页，没有重写三栏编辑器。
- 增加不复核、标准复核、严格复核三种模式，以及独立的生成/审阅供应商、模型、Skill 和版本展示。
- 审阅上下文显式包含用户要求、已确认诊断卡、授权材料、生成版本和证据绑定；禁止仅凭模型知识判断文献支持。
- 审阅只生成报告；用户可选择部分问题、接受原版、填写理由忽略或更换审阅模型。
- 严格流程以 V4、R4、V5、F5 展示追加版本，最多一次自动修订和一次最终验证。
- 增加 REVIEW_FAILED 演示并保留生成版本；完成 5 张审核截图和正式报告。
- 没有修改 Schema、执行迁移、接入真实双模型、新增 Skill 或生产部署。

## 2026-07-27 · M3 数据契约与基础持久化

- 用户批准 V0.4.2 增量 Mock，允许在不重写 M2 前端的前提下进入 M3。
- 新增 37 表 Drizzle Schema 和首次 D1 迁移，覆盖核心写作域及 V0.4.2 六类扩展能力的基础记录；没有启用协作表。
- `.openai/hosting.json` 只声明 D1 `DB`，R2 继续为 `null`；未创建站点、未保存版本、未部署。
- 新增平台身份解析、用户初始化、项目所有者隔离，以及项目、工作区、诊断、提纲和章节版本 API。
- 诊断、提纲、人工保存和恢复均追加新版本；恢复旧版不覆盖历史。
- 前端通过 `NEXT_PUBLIC_M3_PERSISTENCE_ENABLED` 接入基础 D1 Adapter，默认关闭时恢复完整 M2。
- AI、材料上传与解析、外部检索、证据验证和真实 DOCX 仍为 Mock 或未实现。
- 完成本地迁移、双用户隔离、匿名 401、平台身份 200、重启持久化、类型、Lint、构建和自动化测试。

## 2026-07-26 · V0.4.2 增量 Mock

- 用户批准 M2，将其冻结为核心写作工作台与视觉设计基线。
- 从 M2 提交 `5234134` 建立独立分支 `v0.4.2/incremental-mock`，没有改写 M2 历史。
- 主导航只增加一个“研究扩展”聚合入口；编辑器右栏增加默认收起的六页面入口。
- 新增 Idea 探索、外部文献、高级审稿、投稿返修、科研图件和 PPT 六个独立 Mock 页面。
- 使用 `NEXT_PUBLIC_V042_INCREMENTAL_MOCK` 统一控制全部新增设计；关闭态实测恢复 M2 导航和页面。
- 完成 1440×900、390×844 浏览器检查、9 张截图和正式审核报告。
- 未修改 Schema，未执行迁移，未接入真实新增 Skill、外部服务或部署。

## 2026-07-26 · M2 审核门收尾

- 创建 `docs/reviews/M2/review.md`，索引 32 张截图并说明路由、演示流程、浏览器结果、Mock 边界和未实现内容。
- 精确清理重复开发服务器；使用固定 3000 端口单实例完成桌面与窄屏复测后停止。
- 浏览器控制台、服务端 stderr 均为空，未再出现 multiple renderer 警告。
- 管理状态统一为 `WAITING_FOR_REVIEW`；M3、V0.4/V0.4.1、数据库、迁移、新 Skill 和部署均未启动。

## 2026-07-25 · M2 编辑器独立滚动与证据联动

- 全局顶栏、Mock 条和可选提示条固定在三栏工作区之外，页面 Body 不再承担三栏纵向滚动。
- 左侧目录、中间连续纸张编辑区、右侧 AI 工作台分别独立滚动。
- 目录点击可定位章节；中栏滚动会更新目录高亮和章节 URL。
- 正文证据标记与右栏证据卡支持双向定位和高亮。
- 右栏材料授权、引用证据、任务记录改为标签页，任务状态固定在右栏底部且不遮挡内容。
- 左右栏支持折叠和专注写作；390px 窄屏改用目录与 AI 工作台抽屉。

## 2026-07-24 · M2 AI 工作台与可点击闭环

- 将编辑器右侧“研究助手”统一改为“AI 工作台”。
- 六项产品 Skill 按“项目规划 / 写作与资料 / 检查与验证”分组。
- 新增任务准备状态，动态显示诊断卡、当前章节、授权材料与可读材料数量。
- Skill 根据项目状态动态启用；“下一步”进入任务配置，不直接运行模型。
- 修复目录页底部确认条遮挡章节行的问题。
- 完成创建、诊断、目录、编辑、版本、证据、DOCX 预检和管理员四页的 M2 Mock 闭环。

## 2026-07-24 · M1 用户批准与 M2 补充规则

- 用户明确回复“继续”，M1 视觉基线通过。
- 诊断卡确认后允许修改；修改形成新草稿并要求重新确认。
- 系统可从论文要求开始组织论文，但按诊断、提纲、逐章生成推进，禁止伪造文献、数据或结果。

## 2026-07-24 · M1 页面化与审核包

- 将单页内存视图拆分为登录、项目列表、创建方式、五个创建表单、诊断卡和编辑器真实路由。
- 延续用户认可的绿色纸张感视觉。
- 保留“导入已有初稿”文字 `#FFFBE2`。
- 保留“导入文献与范文”背景 `#185208`，文字和字标圆圈 `#FFFBE2`。
- 编辑器统一展示六个产品级 Skill，并增加诊断未确认阻断、材料授权和证据范围。
- 增加 14 张桌面/窄屏真实浏览器截图和 M1 审核报告。
- 修复 Mock 登录以 GET 传递账号和密码的问题。

| Change ID | 日期 | 类型 | 请求/变更 | 影响范围 | 状态 | 验收方式 |
|---|---|---|---|---|---|---|
| CR-001 | 2026-07-24 | 范围内调整 | 五种项目创建方式全部保留 | 创建流程、验收 | ACCEPTED | 五个入口均可见 |
| CR-002 | 2026-07-24 | 范围内调整 | 第一阶段仅导出 Word/DOCX | 编辑器、导出 | ACCEPTED | 不出现 PDF/Markdown |
| CR-003 | 2026-07-24 | 范围内调整 | 排除项目协作与账号共享 | 导航、数据契约 | ACCEPTED | 不出现共享入口 |
| CR-004 | 2026-07-24 | 视觉调整 | 新建项目卡采用绿色树系配色 | 工作台创建卡 | IMPLEMENTED | 浏览器视觉审核 |
| CR-005 | 2026-07-24 | 视觉调整 | “初稿”字色 `#FFFBE2`；“文献与范文”底色 `#185208`、字色和圆圈 `#FFFBE2` | 两张创建卡 | IMPLEMENTED | 浏览器视觉审核 |
| CR-006 | 2026-07-24 | 治理变更 | 按阶段门控文件执行，每个里程碑完成后停止等待审核 | 全项目 | IMPLEMENTED | M1/M2 审核包与 Git 检查点 |
| CR-007 | 2026-07-24 | 信息架构调整 | “章节助手/研究助手”改为“AI 工作台”，六 Skill 分组并显示任务准备状态 | 编辑器右栏、移动端抽屉 | IMPLEMENTED | 桌面/390px 浏览器渲染 |
| CR-008 | 2026-07-24 | 交互规则调整 | 六 Skill 根据诊断、章节和授权材料状态动态启用 | 编辑器任务入口 | IMPLEMENTED | 未确认/已确认两态浏览器断言 |
| CR-009 | 2026-07-25 | 编辑器结构调整 | 三栏固定在视口剩余高度内独立滚动，增加章节/证据双向联动、右栏标签页、折叠、专注模式和窄屏抽屉 | 编辑器桌面端与窄屏端 | IMPLEMENTED | 4 类指定交互截图、Playwright 几何与状态断言 |
| CR-010 | 2026-07-26 | 阶段收尾 | 只完成 M2 审核报告、管理状态、干净复测与 Git 检查点，不进入 M3 | M2 审核包与项目管理文档 | IMPLEMENTED | `docs/reviews/M2/review.md`、进程/端口检查、提交记录 |
| CR-011 | 2026-07-26 | 增量原型 | 冻结 M2 基线，以功能开关增加研究扩展导航、六个独立业务页面和右栏收纳入口 | `app/extensions/**`、主导航、编辑器右栏、审核文档 | IMPLEMENTED | 关闭态 M2 回归、六路由渲染、9 张截图与 `docs/reviews/V0.4.2/review.md` |
| CR-012 | 2026-07-27 | 阶段批准 | 用户确认 V0.4.2 增量 Mock 无问题并连续指示继续，允许进入 M3 | 阶段状态、分支与 WBS | ACCEPTED | M3 分支与审核门记录 |
| CR-013 | 2026-07-27 | 基础持久化 | 在 M2 前端后增加可关闭的 D1 数据层，保留 Mock/真实能力边界 | Schema、迁移、API、Adapter、审核文档 | IMPLEMENTED | 迁移、隔离、重启持久化、构建和测试 |
| CR-014 | 2026-07-27 | 增量原型 | 增加可关闭的双模型生成与独立审阅 Mock，用户控制采纳且版本只追加 | 编辑器右栏、Mock 契约、审核文档 | IMPLEMENTED | 严格复核点击链路、5 张截图、构建与测试 |

## 2026-07-31 · M10 管理与发布准备

- 将管理员用户、项目/文件、任务和模型/Skill 页面接入真实只读数据与受审计操作。
- 增加管理员授权、操作审计、功能开关、实验和运行事件契约及 0017 增量迁移。
- 增加真实模型配置页、会话响应入口、真实 AI 工作台基础、导出下载和 M8/M9 项目入口。
- 升级 Next.js 至 16.2.11，并通过生产依赖审计。
- 增加 Release audit、本地 D1 只读盘点、M5 人工验收说明和 M10/M11 发布/回滚手册。
- M10 严格结论仍为 `M10_BLOCKED`：真实 DeepSeek 未验证、候选版本生产闭环为 PARTIAL、Sites project_id 与 R2 生产绑定缺失；未进入生产部署。
