# M3 当前状态审计

审计日期：2026-07-27

## 结论

`E:\论文系统\site` 仍是唯一活动工程。M2 提交 `5234134` 继续作为核心写作工作台与视觉基线；V0.4.2 增量 Mock 已获用户批准；当前分支为 `m3/data-contract-persistence`。

M3 已完成数据模型、首次迁移、基础 API、所有者隔离和可关闭的前端 D1 Adapter。当前没有进入 M4，没有接入真实文件服务、AI Provider、外部检索、证据验证或 DOCX 产物，也没有部署。

## 已落盘能力

- 37 张 SQLite/D1 表和 Drizzle 首次迁移。
- `.openai/hosting.json` 声明 D1 绑定 `DB`；R2 为 `null`。
- 平台认证头解析；本地演示身份必须显式双开关。
- 项目创建与列表、工作区读取、诊断版本、提纲版本、章节版本 API。
- 所有项目域读写都以 `owner_user_id` 约束。
- 诊断、提纲、人工保存和恢复采用追加版本。
- 前端持久化开关默认关闭；开启后只水合基础写作数据。

## 当前真实数据与 Mock 的分界

| 区域 | 当前状态 |
|---|---|
| 用户身份 | 平台头真实解析；完整登录会话仍待 M4 |
| 项目、诊断、提纲、章节版本 | D1 基础持久化可用 |
| 项目列表与五种创建表单 | 仍以 M2 Mock 交互为主，未完成全站 API 接线 |
| 材料 | 只有数据模型与工作区元数据读取；无上传、R2 或解析 |
| AI 与六个产品 Skill | 只有表结构/Mock 前端；无真实执行 |
| 引用与证据 | 只有表结构/Mock 卡片；无真实解析绑定与外部验证 |
| DOCX | 仍是预检 Mock；无真实文件产物 |
| V0.4.2 六类扩展 | 独立 Mock 页面和基础表；无真实外部服务 |

## 工程状态

- 活动分支：`m3/data-contract-persistence`
- M3 基线：`a044145`
- 当前门：`WAITING_FOR_REVIEW`
- 生产部署：未授权、未执行
- M4：未启动
