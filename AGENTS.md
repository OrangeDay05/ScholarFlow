# 论文 AI 系统开发约束

## 活动工程

- 当前唯一活动工程：`E:\论文系统\site`
- `E:\论文系统\web` 是历史脚手架，只做只读参考，不在本阶段修改或运行。
- 产品事实来源：`E:\论文系统\论文AI系统需求说明_V0.3_简易通版.md`
- 阶段门控来源：`E:\论文系统\论文AI系统_Codex阶段门控项目管理开发指令_V1.0.txt`
- Skill 映射来源：`E:\论文系统\给系统开发Agent的Skill集成说明.md`

## 当前阶段

- 用户已明确批准 M2，将其作为核心写作工作台和视觉设计基线。
- V0.4.2 增量 Mock 与 M3 数据契约已完成并提交；M3 基线提交为 `dc641b8`。
- 当前只实现 V0.4.3 双模型生成与独立审阅前端 Mock。
- 不得推倒、重写或覆盖 M2 页面；所有新增设计必须受统一功能开关控制。
- V0.4.3 前端 Mock 完成后必须停下等待用户审核。
- 用户批准前禁止为双模型能力修改正式 Schema、执行迁移、接入真实模型或新增 Skill、外部服务或生产部署。
- 诊断卡确认后仍可修改；修改会形成待重新确认的草稿，不覆盖此前已确认版本。

## 产品边界

- 项目创建方式固定为五种：Idea、已有初稿、上传论文要求、文献与范文、上传数据。
- 首版只输出 Word，不输出 PDF。
- 不允许共享账号；不支持项目共享或多人协作。
- 首版文件类型应覆盖 Word、PDF、Excel、CSV、TXT 与图片；M2 只模拟队列与状态，不做真实解析。
- 产品级 Skill 只允许展示六个：项目诊断与提纲、文献总结与矩阵、章节写作、通用改稿、全文一致性检查、引用与证据检查。
- 供应商、真实数据库与成本未确定的能力必须标为“规划中”或 Mock，不得伪装为已接入。

## 视觉约束

- 主色沿用绿色体系：`#14543A`、`#348E38`、`#007746`、`#185208`、`#7EBD9E`、`#D7EADA`。
- “导入已有初稿”卡片文字使用 `#FFFBE2`。
- “导入文献与范文”卡片背景使用 `#185208`，文字与字标圆圈使用 `#FFFBE2`。
- 页面必须同时检查桌面 `1440×900` 与窄屏 `390×844`。

## 代码与验证

- 只做当前 Gate 所需的最小改动，不扩展未要求的功能。
- 共享组件放在 `app/components/`，Mock 数据和契约放在 `app/lib/`，页面私有样式优先使用 CSS Modules。
- 任何“已完成”都必须有对应构建、浏览器渲染、点击、日志或截图证据。
- M2 证据保留在 `docs/reviews/M2/`。
- V0.4.2 增量 Mock 证据写入 `docs/reviews/V0.4.2/`，截图写入其 `screenshots/`。
- V0.4.3 双模型复核 Mock 证据写入 `docs/reviews/V0.4.3-dual-model-review/`。
- 不修改用户无关文件，不删除历史工程，不覆盖未归属的并行 Agent 文件。

## 常用命令

```powershell
$env:CI='true'
pnpm run build
pnpm run lint
```
